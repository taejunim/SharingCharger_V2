<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/white">

        <include
            android:id="@+id/include_header"
            layout="@layout/layout_header"
            android:layout_width="fill_parent"
            android:layout_height="46dp" />

        <include
            android:id="@+id/include_charger_manage_menu"
            layout="@layout/layout_charger_manage_detail_header"
            android:layout_width="match_parent"
            android:layout_below="@id/include_header"
            android:layout_height="wrap_content" />


        <RelativeLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/include_charger_manage_menu"
            android:layout_above="@+id/include_footer"
            android:layout_marginTop="0dp" />

        <include
            android:id="@+id/include_footer"
            layout="@layout/layout_owner_footer"
            android:layout_width="match_parent"
            android:layout_height="46dp"
            android:layout_alignParentBottom="true" />
    </RelativeLayout>

</layout>